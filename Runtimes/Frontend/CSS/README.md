# CSS

## 说明

CSS 运行时约束，适用于页面样式、后台样式、组件样式和局部交互样式。

## 规则

- 优先复用现有 class 和样式约定
- 避免无范围的全局选择器
- 不为了局部需求重写整站样式
- 样式优先写在 CSS、样式文件或项目既有样式体系中
- 不随意写内联样式
- 修改样式时必须考虑列表、表单、弹窗、分页等常见后台场景
- 不默认引入新的 CSS 框架

## TODO

- Future: legacy admin UI conventions
- Future: responsive compatibility checklist
